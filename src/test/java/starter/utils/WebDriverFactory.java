package starter.utils;

public class WebDriverFactory {
/*
    // Método estático para crear el WebDriver según el navegador
    public static WebDriver createDriver(String browser) {
        WebDriver driver;

        // Configuración automática del WebDriver usando WebDriverManager
        if (browser.equalsIgnoreCase("chrome")) {
            WebDriverManager.chromedriver().setup();  // Configura el ChromeDriver automáticamente
            driver = new ChromeDriver();
        } else if (browser.equalsIgnoreCase("firefox")) {
            WebDriverManager.firefoxdriver().setup(); // Configura el GeckoDriver automáticamente
            driver = new FirefoxDriver();
        } else {
            throw new IllegalArgumentException("Unsupported browser: " + browser);
        }

        return driver;
    }*/
}
